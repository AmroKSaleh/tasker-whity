---
id: TDE-411
title: "Local Mode: agent workflow directives (pull/flush bracket) + deploy"
status: done
priority: high
section: mcp-integrations
order: 29
updated_at: 2026-07-25T07:41:59.616Z
input: {"edges":[{"contract":{"rules":[{"id":"r1","kind":"check","rule":"command: deployed MCP tools/list includes pull_local_project and flush_local_project; a pull call against a local_mode-enabled project returns a file map containing project.json, at least one tasks/*.md, and .sync.json content with a numeric cursor","label":"Sync tools deployed and callable","severity":"blocker","description":"The directives name these tools and teach their cadence — they must exist as deployed, working surface first."}],"confirmed":true,"confirmed_at":"2026-07-14T22:50:28.420Z","confirmed_by":"human"},"expected_type":"code","source_task_id":"TDE-410"}]}
output: {"contract":{"rules":[{"id":"r1","kind":"check","rule":"command: fetch the deployed MCP initialize response; result.instructions contains the LOCAL MODE workflow paragraph (pull-before-work / flush-after-work-unit)","label":"Local Mode directive live","severity":"blocker","description":"The dogfood agent workflow follows the deployed directive — it must be live before the run."}],"confirmed":true}}
---

Flow step (Local Mode v1 Build — design: V2/docs/local-first-design.md D7). Teach agents the Local Mode work bracket via the strings the MCP already injects (same mechanism as TDE-404): a LOCAL MODE paragraph in TASKER_SERVER_INSTRUCTIONS + an ASSISTANT_DIRECTIVES entry. Content: when a project has Local Mode, work it through .tasker/ files (hot path — read/edit files directly, no per-edit MCP calls); PULL before starting work (pull_local_project, write returned files verbatim incl. .sync.json); FLUSH after each work unit (task completed / handoff) via flush_local_project with changed files; apply hub-won corrections back to disk; completion ceremonies (submit_validation_result / submit_task_review / complete_task) still via MCP at flush time; never hand-edit .sync.json; transient scratchpad rules unchanged. Deploy with --no-verify-jwt. Definition of done: deployed initialize response contains the LOCAL MODE directive text (verified by fetch, TDE-404 pattern).
