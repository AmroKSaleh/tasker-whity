---
id: TDE-244
title: Deploy mcp edge function to activate create_project's AI-propose IS nudge (TDE-193 tail)
status: done
priority: low
section: core-app
order: 41
updated_at: 2026-07-25T07:41:59.616Z
---

Tail of TDE-193. The baseline Instruction Set is LIVE and verified (DB trigger seeds Task hygiene + Working preferences on every new project — confirmed 2 entries on a UI-created project 2026-06-13).

What's left is purely cosmetic and already coded in the repo, just not deployed:
- V2/supabase/functions/mcp/index.ts create_project handler now (a) states the baseline was auto-applied and (b) nudges the assistant to propose 2–4 project-specific IS additions for the user to confirm via create_is_entry — the "AI-proposed" half of the feature.
- The create_project tool description was updated to match.

ACTION: next time you deploy the mcp function, this ships automatically — npx supabase functions deploy mcp --no-verify-jwt (from V2/). No DB change needed. No rush; the baseline already works without it. NOTE: the currently-deployed mcp code is the original (never inserted IS itself), so there is no double-seeding risk before or after this deploy — the trigger remains the sole seeder.
