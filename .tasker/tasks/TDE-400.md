---
id: TDE-400
title: Zero-cost autonomous work loop — /work-loop driven by /loop on the subscription
status: done
priority: medium
section: mcp-integrations
order: 18
updated_at: 2026-08-07T11:33:13.117Z
---

SHIPPED 2026-07-13. Path A of the autonomy loop — the zero-marginal-cost alternative to the cloud receiver (TDE-398, parked).

WHAT: a `/work-loop` Claude Code command (~/.claude/commands/work-loop.md) tuned for UNATTENDED interval execution, driven by the `/loop` skill: `/loop 2m /work-loop`. Runs inside Claude Code on the user's flat-rate subscription → $0 marginal API cost (polls count against the plan's usage allowance, not real dollars). Each pass: get_ready_work → execute ALL confirmed proposals (group A) → prepare the top handed-over task (group B) and stop for async web confirm.

DIFFERS FROM /work-ready: /work-ready is interactive (one-shot, human watching, "present in chat / tell them / run again"). /work-loop is unattended-safe: never calls AskUserQuestion or waits for chat, quiet on empty queue, skip-and-flag on blockers/ambiguity (never guesses on destructive actions), honors stop_flow (TDE-384).

CONSTRAINT (the accepted tradeoff): runs only while a Claude Code session is OPEN and the PC is on. True PC-closed autonomy requires cloud compute = API cost (TDE-398, parked for that day). User explicitly chose zero-cost over PC-closed.

VERIFIED: empty-queue pass returns a clean "nothing to do" (no chatter); command registered as a skill without a restart. Full end-to-end (hand → confirm → loop executes) is the user's to exercise live.
