---
id: TDE-382
title: Guided review narrative — restructure judge output for the human gate
status: pending
priority: medium
section: agent-native-layer
order: 8
updated_at: 2026-07-14T23:09:51.590Z
---

From Linear: Guided Reviews (2026-05-27, with Linear Diffs) restructure a PR into a narrative — core of the change first, per-section explanations (what changed → consequences), glue/secondary code separated out — so review shifts from error-hunting to judgment. Their companion essay's thesis: agents made review the bottleneck (50% more PRs Jan–Mar 2026); humans should judge fit, not hunt typos. That thesis IS our positioning ("quality-gated").

Build: the existing task judge (review_task / get_task_critique) emits a structured review narrative instead of a flat verdict blob: the core claim/change first, then consequence-ordered sections, then secondary noise separated at the bottom. Verdict logic unchanged — presentation upgraded so the human gate takes 30 seconds of judgment. Render in the inspector on the needs-review card.

We do NOT build a diff/code-review surface (duplicates GitHub; team-scale). Cross-ref TDE-261 (judge design, open) and TDE-348 (needs-review card state, in progress).

## Build spec — locked 2026-07-11 (my recommendation)
- The existing judge (review_task / get_task_critique) already produces review_verdict (per-rule results + critique). This task changes only the OUTPUT SHAPE, not the verdict logic.
- Give the critique a narrative structure: `{ core: <the central change/claim>, sections: [{ change, consequence }], secondary: <glue/trivia separated out> }`. The judge prompt asks for exactly this shape.
- Render on the needs-review card: ReviewVerdictPanel.jsx (inspector) + the get_task review-verdict block. Core first, consequences next, secondary collapsed at the bottom — so the human gate is ~30s of judgment, not typo-hunting.
- Do NOT build a diff/code-review surface (duplicates GitHub; team-scale).
Depends on the open judge work (TDE-261) + the review card (TDE-348). Small–medium. Independent of TDE-374.

## SHIPPED + VERIFIED LIVE — 2026-07-11 (PR #37)
All 3 milestones done. The judge emits review_verdict.narrative {core, sections:[{point,consequence}], secondary}; verdict logic unchanged. review_task prompt asks for it; submit_task_review stores it; get_task + ReviewVerdictPanel + Front Page needs-review card render it. Verified end-to-end via a real review on a scratch task — get_task rendered "▸ Core / → point — consequence / Secondary" correctly. Additive (new key in existing review_verdict JSON, no migration). Frontend panels build-verified + deployed (user does the visual check on a real escalated review).
