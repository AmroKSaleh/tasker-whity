---
id: TDE-235
title: Use the MCP handshake to teach AI platforms the optimal way to use Tasker
status: done
priority: medium
section: mcp-integrations
order: 21
updated_at: 2026-07-14T23:09:51.590Z
---

DONE & verified end-to-end (2026-06-10).

Implemented (all deployed to Supabase, committed in dea86ff):
- initialize.instructions (TASKER_SERVER_INSTRUCTIONS): mental model + first-move + always-on rules, loaded at connection with no tool call.
- list_projects description cues __init_tasker_session as a backstop.
- Tool-description audit: create_task→build_new_flow cue; update_task/complete_task hard-block + gating warnings; destructive deletes carry confirm cues; analyze_section↔section_insights disambiguation; __init_tasker_session reframed "call FIRST".

VERIFICATION: a fresh Claude Code session in a DIFFERENT project (no Tasker memory present) was cold-asked "what do you know about how to use Tasker?" without calling tools. It reproduced the exact hierarchy framing (no "gearbox" = proves it came from the MCP instructions, not memory snapshots), the first-move (call __init_tasker_session), the rules (in_progress lifecycle, hard-enforced deps, short IDs, hide done, flows for multi-step), AND an accurate tool-surface summary from tools/list. Clean pass. Confirms Claude Code honors initialize.instructions.

NOTE: first attempt was contaminated — testing in the Tasker repo itself, the model answered from project auto-memory ("gearbox" framing) and the MCP server hadn't finished connecting. Lesson: to test the handshake, use a clean project + wait for the server to connect.

FOLLOW-UP (optional, not blocking): verify Cursor / Windsurf / Roo also surface initialize.instructions (vs relying on the list_projects backstop). Claude Code confirmed.
