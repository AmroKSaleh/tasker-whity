---
id: TDE-809
title: Correct the two stale user-authored KB entries (Tech Stack, Phase 1 &amp; Phase 2 Plan)
status: done
priority: medium
section: core-app
order: 116
updated_at: 2026-07-31T13:45:34.757Z
milestones:
  - { "text": "Correct the Tech Stack entry against V2/app/package.json and add the missing current surface", "done": false }
  - { "text": "Correct the Phase 1 & Phase 2 Plan entry with a dated superseded line pointing at KB entry b65fb8c5", "done": false }
  - { "text": "Sweep the remaining KB entries for other pre-V2 claims and list what is found", "done": false }
  - { "text": "Log any larger cleanup as a separate task rather than absorbing it here", "done": false }
---

WHY THIS IS NOT COSMETIC: these are the entries agents pull to ground themselves at the start of work. Both now assert things that are false, so the KB is actively misleading new sessions — the opposite of what the dynamic-KB rule (TDE-254) is for.

ENTRY 1 — "Tech Stack" (id f5cca033-32af-4acf-a4ba-0aa6ce0a0177, source: user): says React 18, React Router v6, and describes Gemini as the AI layer. Actual (verified in V2/app/package.json): React 19.2, React Router v7.15, Vite 8, Tailwind 3.4, Zustand 5, plus @anthropic-ai/sdk and @supabase/supabase-js 2.105. Also omits the entire current surface — flows, environments, orgs, connectors, Local Mode, Capacitor.

ENTRY 2 — "Phase 1 & Phase 2 Plan" (id 36e98c7a-df3f-4cc1-95f6-28f81401209e, source: user): still lists "Remove in-app AI (Option 1 architecture)" as the plan. That was RETIRED — see KB entry b65fb8c5 "{product} Decision: in-app AI is INTENTIONAL in V2 — Option-1 removal (TDE-123) retired". An agent reading the plan entry without the retirement entry will strip features on purpose.

FIX APPROACH: correct in place rather than delete, and add a dated "superseded" line where a decision changed, so the history stays legible. Do not silently rewrite a user-authored entry's conclusions — mark what changed and when.

CONNECTED TASKS:
- TDE-254 — the dynamic-KB read loop these entries are served through
- TDE-123 — the in-app-AI removal that was retired
- TDE-371 — KB index is shipped on get_task, so stale titles cost tokens AND accuracy
- Sibling: the task marking V2/ARCHITECTURE_BRIEF.md and V2/AI_PRIMER.md as historical (same class of problem, in files instead of the KB)

WHILE IN THERE: sweep the other 70 entries for pre-V2 claims of the same kind, but do not expand this task into a full KB audit — log anything larger as its own task.

CONNECTED (short IDs, added 2026-07-27): sibling TDE-810 — same problem class (stale grounding material), files instead of KB entries. Both should close before the repo goes public for TG-55.
