---
id: TDE-408
title: "Local Mode: .tasker/ format loader/serializer"
status: done
priority: high
section: mcp-integrations
order: 27
updated_at: 2026-07-25T07:41:59.616Z
output: {"contract":{"rules":[{"id":"r1","kind":"check","rule":"command: run the serializer round-trip test (node --experimental-strip-types) against the reference .tasker/ example — parse to model, serialize, reparse, deep-equal the two models and the regenerated file set; expect exit 0","label":"Lossless round-trip proven","severity":"blocker","description":"The sync engine trusts the serializer blindly; any lossy field silently corrupts every sync."}],"confirmed":true}}
---

Flow step (Local Mode v1 Build — design: V2/docs/local-first-design.md, format: V2/docs/tasker-file-format.md). Build the pure serialization module the sync engine consumes: DB task/project rows ↔ .tasker/ file texts (project.json + tasks/<ID>.md with YAML frontmatter). Implements the format spec + the 5 D2 deltas: updated_at in frontmatter, contract-bearing edges (simple input.from form OR full edges-with-rules form), output as string or object-with-contract, review.bar carriage, omit-don't-null. Pure functions, no I/O, Deno-and-Node compatible TS placed beside the MCP fn (V2/supabase/functions/mcp/local_format.ts) so index.ts imports it directly. Reuse contract_gate.ts patterns. Definition of done: lossless round-trip on the reference example V2/examples/blog-post-workflow/.tasker (parse → model → serialize → reparse → deep-equal), test script exits 0 via node --experimental-strip-types.
