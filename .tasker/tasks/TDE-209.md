---
id: TDE-209
title: Update BlueprintView to the new edge-list (fan-in) format
status: done
priority: medium
section: flows
order: 12
updated_at: 2026-07-14T23:09:51.590Z
---

WEAKNESS: the project-wide BlueprintView still reads `input.source_task_id` (legacy single-source), so it cannot render fan-in — a flow's merges don't show there, and it's now inconsistent with the Flows page.

FIX: switch BlueprintView's flow detection / layout / edge building to the shared `inputEdges()` helper already used by the Flows page (lib/flowGraph.js). Low effort; removes the inconsistency between the two graph surfaces.
