---
id: TDE-381
title: Project updates — health status + auto-computed delta since last update
status: done
priority: medium
section: core-app
order: 89
updated_at: 2026-08-07T07:34:29.432Z
review: {"bar":{"rules":[{"id":"tool-and-server-delta","kind":"check","rule":"The MCP tool must exist and the delta must be computed server-side and attached automatically — not passed in by the caller.","label":"post_project_update exists and the SERVER computes the delta","severity":"blocker","description":null},{"id":"front-page-pinned-plus-history","kind":"check","rule":"The front page must render the latest published update with a health indicator and offer a history view of earlier ones.","label":"Latest update pinned on the Front Page with health colour, plus history","severity":"blocker","description":null},{"id":"agent-drafts-human-publishes","kind":"check","rule":"An agent must be able to read the computed delta, draft an update from it, and leave it for a human to publish from the web UI.","label":"Agent drafts from the delta, human publishes","severity":"blocker","description":null}],"source":"task_text+IS+KB","frozen_at":"2026-08-07T07:34:04.778Z"}}
milestones:
  - { "text": "post_project_update MCP tool + server-side delta computation", "done": false }
  - { "text": "Front Page: pinned latest update with health color + history view", "done": false }
  - { "text": "Agent-drafted update flow (draft from delta, human publishes)", "done": false }
---

From Linear: project updates are structured posts with a mandatory health enum (On track / At risk / Off track) + rich text; Linear AUTO-ATTACHES the delta since the last update — target-date changes, lead changes, milestone progress, overall progress when it moved >2%. Latest update pins to the project overview; a "Write with Agent" button drafts the update from changes + linked discussion, human edits and publishes. The delta engine is the sleeper feature — the research's strongest single adaptation.

Build: post_project_update MCP tool (health: on_track|at_risk|off_track, body); the SERVER computes and attaches the delta since the last update (tasks completed, sections progressed, dates slipped, new/blocked items); latest update pinned to the Front Page lead card with health color; history on the project. Agent-drafted flow: agent reads the computed delta, drafts, human publishes.

Extends Day Summary (TDE-160) from day-scoped to project-scoped, and gives cross-session recaps durable anchors — human-shared state narrated for humans. Related: TDE-251 (AI-as-dashboard evaluation).
