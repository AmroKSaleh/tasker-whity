---
id: TDE-366
title: Org member invitations — copy-link invite → accept → membership (SHIPPED)
status: done
priority: medium
section: backlog
order: 65
updated_at: 2026-07-14T23:09:51.590Z
---

SHIPPED + deployed 2026-07-06. Org-layer slice 2: an org admin invites people by email; membership is created when they accept.

Both open questions settled with the user:
1. Email delivery = COPY-LINK MVP (no transactional email provider exists in the project). Creating an invite copies /invite/<token> to the clipboard; Resend is the later swap (last mile only).
2. Not-yet-registered emails: the INVITATION ROW is the pending state (keyed by email); membership is created only at accept, when a real user_id exists — one mechanism for both on-Tasker and not-on-Tasker invitees. Grants deferred: invite dialog is email + role only; per-env grant assignment is a later action.

Delivered: migration 20260706120000_org_invitations.sql (organization_invitations table + RLS + 3 SECURITY DEFINER RPCs preview/accept/list_org_members + org_members DELETE policy); lib/invitations.js; public /invite/:token InvitePage; OrgMembers panel in OrgCard; AuthGuard localStorage catch for OAuth logins. Backend RPCs verified via SQL; frontend built + deployed to Netlify. Full detail in KB 'Org member invitations — copy-link flow (TDE-361)'.

Follow-ups (NOT in this task): per-env grant assignment UI; MCP in-code grant enforcement (TDE-359); Resend email sending; optional email-match enforcement on accept.
