---
id: TDE-158
title: Make the breadcrumbs above the project title clickable for navigation
status: done
priority: medium
section: core-app
order: 6
updated_at: 2026-08-08T05:07:38.190Z
review: {"bar":{"rules":[{"id":"r1","kind":"check","rule":"command: `npm run build` in app/ exits 0. Record the exit code and the tail of stdout.","label":"Build passes","severity":"blocker","description":null},{"id":"r2","kind":"check","rule":"Fetch the live project board URL on Netlify and confirm the rendered HTML contains the environment name AND an anchor with href=\"/environments\". Record the matched HTML snippet verbatim as observed_value.","label":"Trail renders as real links on live","severity":"blocker","description":null},{"id":"r3","kind":"check","rule":"grep -c '← Home' app/src/components/board/ProjectHeader.jsx returns 0. Record the actual count.","label":"Broken back-link removed","severity":"blocker","description":null}],"source":"task_text+IS+KB","frozen_at":"2026-08-07T16:16:23.222Z"}}
---

The breadcrumb trail displayed above the project title is currently not interactive. Make each breadcrumb segment a clickable link so users can navigate up the hierarchy (e.g. back to the projects list or parent view) directly from it.

## Scope expanded 2026-07-31 — owner confirmed this still needs doing, wants breadcrumbs used site-wide, not just above the project title.

IMPORTANT CORRECTION: checked the current codebase and found NO breadcrumb element anywhere — not in ProjectBoard.jsx's masthead (just an H1 project title + a row of action buttons, no navigational trail above it), not in any page wrapper (Dashboard/layout components). The board went through a full rebuild on 2026-07-01 (TDE-350, "floor plan v1"), a month after this task was filed — the old breadcrumb this task describes as "currently not interactive" most likely didn't survive that rebuild in any form.

Practical consequence: this is probably no longer "make an existing static breadcrumb clickable." It's now "design and build a breadcrumb trail from scratch" (e.g. Environment / Project / Section, or similar, matching whatever hierarchy makes sense post-rebuild), make it clickable, THEN expand that pattern to other pages across the site per the owner's direction. Scope is bigger than the original one-liner assumed — worth a real design pass (what should the trail actually show at each page level?) before building, not a quick CSS-only fix.

Not started — recording the corrected scope so whoever picks this up doesn't assume the old premise still holds.
