---
id: TDE-412
title: "Local Mode: web opt-in UI (creation flag + settings toggle + badge)"
status: done
priority: high
section: core-app
order: 111
updated_at: 2026-07-25T07:41:59.616Z
input: {"edges":[{"contract":{"rules":[{"id":"r1","kind":"check","rule":"command: SQL introspection against the linked prod DB returns the projects.local_mode boolean column (the value the toggle writes)","label":"local_mode column live in prod","severity":"blocker","description":"The toggle writes this flag; without the column every save fails."}],"confirmed":true,"confirmed_at":"2026-07-14T22:50:29.829Z","confirmed_by":"human"},"expected_type":"code","source_task_id":"TDE-409"}]}
output: {"contract":{"rules":[{"id":"r1","kind":"check","rule":"command: the deployed production frontend bundle contains the \"Local Mode\" toggle/badge strings, and the settings toggle path writes projects.local_mode","label":"Opt-in UI live","severity":"blocker","description":"Dogfood begins by enabling Local Mode the way a real user would — the UI entry point must exist on live."}],"confirmed":true}}
---

Flow step (Local Mode v1 Build — design: V2/docs/local-first-design.md, settled answers: opt-in at creation AND settings toggle; owner-only; name exactly "Local Mode"). React app (V2/app, JSX + Tailwind, functional components, bordered buttons per project IS): add a Local Mode opt-in to the project creation modal and a toggle in project settings; write projects.local_mode; show a "Local Mode" badge on the project header/board where other project chips live. Owner-only: hide the toggle for org-owned environments' projects (env org_id set → not owner-solo → hidden). The toggle only marks the project — hydration happens on first pull (per settled answer #3). Deploy: npm run build → npx netlify-cli deploy --prod --dir=dist (straight to live per verify-on-live rule). Definition of done: deployed bundle contains the Local Mode strings; toggle writes the flag.
