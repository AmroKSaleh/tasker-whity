---
id: TDE-413
title: "Local Mode: dogfood on Tasker Development end-to-end"
status: done
priority: high
section: core-app
order: 112
updated_at: 2026-07-25T07:41:59.616Z
input: {"edges":[{"contract":{"rules":[{"id":"r1","kind":"check","rule":"command: run the scripted two-device sync simulation — two devices pull, make concurrent edits to the same task, flush in sequence (LWW resolves to the newest updated_at), a delete propagates via tombstone to the other device, and a concurrent edit beats a delete; expect exit 0 with each scenario asserted","label":"Two-device simulation passes","severity":"blocker","description":"Dogfooding on the real project must not begin until the conflict machinery is proven on synthetic devices."}],"confirmed":true,"confirmed_at":"2026-07-14T22:50:31.250Z","confirmed_by":"human"},"expected_type":"code","source_task_id":"TDE-410"},{"contract":{"rules":[{"id":"r1","kind":"check","rule":"command: fetch the deployed MCP initialize response; result.instructions contains the LOCAL MODE workflow paragraph (pull-before-work / flush-after-work-unit)","label":"Local Mode directive live","severity":"blocker","description":"The dogfood agent workflow follows the deployed directive — it must be live before the run."}],"confirmed":true,"confirmed_at":"2026-07-14T22:50:32.737Z","confirmed_by":"human"},"expected_type":"string","source_task_id":"TDE-411"},{"contract":{"rules":[{"id":"r1","kind":"check","rule":"command: the deployed production frontend bundle contains the \"Local Mode\" toggle/badge strings, and the settings toggle path writes projects.local_mode","label":"Opt-in UI live","severity":"blocker","description":"Dogfood begins by enabling Local Mode the way a real user would — the UI entry point must exist on live."}],"confirmed":true,"confirmed_at":"2026-07-14T22:50:33.375Z","confirmed_by":"human"},"expected_type":"code","source_task_id":"TDE-412"}]}
output: {"contract":{"rules":[{"id":"r1","kind":"check","rule":"command: on the real project — pull hydrates .tasker/ into the repo; a task created and edited purely through files lands in the hub after flush (verified by an MCP read); a second directory with a different device_id pulls and sees it; a concurrent-edit conflict resolves to the newer edit; a delete propagates via tombstone; every step evidenced by raw tool responses","label":"End-to-end dogfood proven live","severity":"blocker","description":"This is the flow goal: Local Mode working on Tasker Development itself, multi-device proven via second directory (user-approved scope)."}],"confirmed":true}}
---

Flow step — the GOAL task (Local Mode v1 Build — design: V2/docs/local-first-design.md; user-approved proof scope: second directory on the same PC is sufficient for v1). Turn Local Mode on for the Tasker Development project itself and prove the full cycle live: pull hydrates .tasker/ into the repo working dir (c:/Users/PC/Desktop/Personal Projects/Tasks App); create + edit a real marker task purely through files; flush; verify the change appears in the hub (list_tasks/get_task); second directory (scratchpad) pulls with a different device_id and sees it; conflicting edits in both dirs resolve by LWW (newer wins); delete propagates via tombstone; edit-beats-delete race verified. Clean up the marker task at the end (through the tombstone path — that IS the test). Evidence: raw tool responses captured for the status report.
