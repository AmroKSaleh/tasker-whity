---
id: TDE-288
title: "Flows cockpit: surface contract blessing + gate status in the web app"
status: done
priority: high
section: flows-optimization
order: 4
updated_at: 2026-07-14T23:09:51.590Z
---

Make the TDE-287 moat legible + actionable in the app (web app = cockpit/inspector per the execution-layer strategy). The app shows contract rules but hides the one property that matters — blessed vs AI-QA'd — and offers no way to bless. Scope:
1. Blessed/AI-QA'd badge on every input + output contract in FlowTaskPanel (✓ human-blessed by/when vs ⊘ AI-QA'd — needs review).
2. A "Bless" action in-app (writes contract.confirmed=true + confirmed_at/by directly via supabase, mirroring MCP confirm_contract). Note: editing a contract resets confirmed=false (matches server), so bless must be re-doable.
3. Gate status on flow cards + FlowDetail header: ✓ all blessed / ⚠ N unblessed (gate would block) / ⛔ gate bypassed (reason) — using flows.gate_bypassed/_reason and a client-side mirror of contractGateViolations.
Frontend only (JSX, Tailwind, Netlify deploy).
