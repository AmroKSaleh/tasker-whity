---
id: TDE-582
title: "\"AI needs review\" board state — remaining slices (rail \"Needs you\" zone + notification badge + final glyph)"
status: pending
priority: medium
section: flows
order: 53
updated_at: 2026-07-25T07:41:59.616Z
---

Follow-up to TDE-348 (split out 2026-07-17). TDE-348 shipped the CORE card slice — indigo design token (--color-review, all 3 themes + dark variant) and BoardCard rendering the escalated state (indigo left bar + tint, "◆ NEEDS REVIEW" chip, one-line failure reason, card = Review→ affordance). This task holds what 348 did NOT build.

REMAINING SCOPE:
- Right-rail "Needs you" zone: aggregate all tasks with review_verdict.escalated === true into a dedicated board-rail zone.
- Left-rail notification badge: driven by the count of escalated tasks (the "Needs you" count feeds the nav badge).
- Finalize the glyph: ◆ is a placeholder — pick a real one.
- Colorblind check on the indigo (#4B57D8 light / #8B93F0 dark) treatment.

KEYS OFF: review_verdict.escalated === true ONLY (the terminal "AI gave up after 3 attempts" state) — NOT every gate failure (a 1st/2nd fail = AI still retrying = in_progress = not the human's problem yet). Rendering every fail would be noise.

SCOPE GUARDS (locked decisions — do not violate):
- NO flow state on the board (user directive; see memory decision-flows-separate-from-project-view). The dropped "second source: flow validation ask_human / retry_blocked" stays DROPPED. This surface is the non-flow task-level judge only (review is mutually exclusive with flow tasks).
- Progressive disclosure (topology full/slimline/+N-more) was mainly for the flow-chained case → not needed here; independent review escalations each render full.

NOT BLOCKED ON BACKEND: review_verdict already persists on the task row; board selects '*' so review_* reaches the client; ReviewVerdictPanel + list-item badge already exist. This is a frontend surfacing job.

POINTERS: framework + approved mockup = TDE-346 (https://claude.ai/code/artifact/221f1d5d-b93c-491e-ab74-758e5c594fc0). Escalation source = TDE-261 (task-level judge; this is effectively the UI for its deferred async-for-human-completion milestone). Files touched in 348: index.css (--color-review), tailwind.config, ProjectBoard.jsx (BoardCard).
