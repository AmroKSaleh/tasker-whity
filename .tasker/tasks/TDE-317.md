---
id: TDE-317
title: Run round 2 (A/C/B) with the calibrated validator → decisive B-vs-C verdict
status: pending
priority: high
section: confirmation-test
order: 2
updated_at: 2026-07-14T23:09:51.590Z
---

THE decisive experiment. Depends on BOTH: "Calibrate the experiment validator" (validator must PASS known-correct code) AND "Design round 2's harder task" (spec-only ~60–80%). Do not run until both are done.

WHAT TO DO: re-run the 3-arm protocol on the round-2 task — Arm A (bare loose prompt), Arm C (full contract, no validator), Arm B (contract + CALIBRATED independent validator + revision loop). N=5 per arm, Sonnet 4.6, via Claude Code subagents (no API — company account can't call APIs), candidates scored LOCALLY against the round-2 hidden oracle. Reuse the round-1 pattern: adapt the workflow script (generation) + score_subagent_output.py (local scoring) to the round-2 task files.

THE DECISIVE METRIC IS B − C (not B − A):
- B − C ≥ ~10pp  => independent validation IS a real moat on hard work. This is the evidence that the verification loop (the one thing free competitors Task Master / Backlog.md lack) actually improves output. Feeds the B2B/teams positioning.
- B ≈ C  => validation is decorative even when a clear spec ISN'T enough => the moat is largely the contract-authoring (which competitors already have) => reposition toward the memory / org-brain / vendor-neutral angle (per the 2026-06-27 market-research verdict). 
Also report consistency (SD) and cost (revisions/tokens).

ON COMPLETION: record results in the pre-reg doc (new "Round 2 results" section) + a KB entry; update TDE-212 and the TG canonical task "Experiment: prove/disprove contract-gated...", and check off its "Round 2" milestone. State the strategic decision plainly (moat confirmed-on-hard-work / decorative).

POINTERS: harness = V2/experiments/contract-gating/ ; round-1 workflow script under V2/.../workflows/scripts/ ; pre-reg = V2/docs/experiments/contract-gating-preregistration.md ; KB "Flows: contract-gating experiment Round 1".
