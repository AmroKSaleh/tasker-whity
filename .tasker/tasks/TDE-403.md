---
id: TDE-403
title: Show all tasks button + add sorting options to each section
status: done
priority: high
section: core-app
order: 83
updated_at: 2026-08-07T19:11:27.842Z
---

## FOUND MOSTLY DONE, FINISHED THE GAP — 2026-07-31

**Per-section sort/status control was already shipped 2026-06-18/07-18 and never marked done.** Commits 0fe0fbb ("Add persistent per-section sort + status filter (TDE-403)") and d003e80 ("Fix task cards overlapping in sorted sections (TDE-403)"), both explicitly tagged TDE-403 in their own commit messages. `SectionViewControls` in ProjectBoard.jsx is a complete, working sort dropdown (Manual/Priority/Due/Created/Status) with a direction toggle and a per-section Pending/All status override, persisted to `sections.view_prefs`. This is the same stale-tracking pattern found repeatedly tonight (pull_google_task, the TDE-780 rename, TDE-263) — code shipped, task never closed. No work needed here.

**The one genuinely missing piece — confirmed with the user, not guessed:** a "show all tasks" toggle to reveal flow-step tasks, which TDE-320 hides from the board entirely by design. Built and deployed:
- New toggle Pill ("Flow Steps", live count of currently-hidden tasks) in both filter locations — the main Stacks strip and the focused-section header.
- Single-point change: `allBoardTasks` was already documented in the code's own comments as "the choke point" for the flow_id exclusion that `boardTasks`/`enrichedSections`/`focusedSection` all derive from, so gating it on a `showFlowSteps` toggle state propagates correctly everywhere without touching downstream logic.
- Mirrors the `include_flow_steps` override already shipped on the MCP's `list_tasks` earlier tonight — same concept, same default (hidden unless explicitly asked for).

Branch `tde-403-flow-steps-toggle` (off main, not the MCP stack — unrelated), commit a1bf0c0, pushed. Deployed to Netlify production.

**VERIFICATION GAP, stated plainly:** production build succeeds cleanly; eslint holds at the pre-existing baseline of 7 errors (confirmed via git stash — 2 unrelated no-unused-vars, 5 pre-existing React Compiler purity/effect-in-render warnings, none in code this touches). **Could NOT get a live authenticated screenshot** — the app has no dev-auth bypass and chromium-cli wasn't available in this environment, so this has not actually been seen rendering in a browser. Needs a manual look on the live site before calling this done.

## VERIFIED LIVE 2026-07-31 — both pieces confirmed working by the owner on production

Flow Steps toggle: appears in the filter row, toggles hidden flow-step tasks on/off correctly.
Per-section sort (the already-shipped-but-unclosed half): sort icon in each section header opens the Manual/Priority/Due/Created/Status dropdown, reorders correctly.

Closing genuinely complete — not just deployed, actually seen working.
