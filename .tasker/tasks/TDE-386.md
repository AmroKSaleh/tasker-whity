---
id: TDE-386
title: @-references — typed links between tasks and KB entries with backlinks
status: pending
priority: low
section: core-app
order: 32
updated_at: 2026-08-07T11:33:35.074Z
milestones:
  - { "text": "Reference extraction on save (task details, notes, KB bodies)", "done": false }
  - { "text": "Backlink rendering on tasks + KB entries", "done": false }
  - { "text": "MCP traversal affordance", "done": false }
---

Context:
Goal 1: Add semantic 6-character short-codes to KB entries (e.g. "DB-SCH"). This replaces UUIDs in the injected KB index, saving >60% token overhead (~700 tokens) on every get_task call.
Goal 2: @-references. Mentioning TDE-X or a KB short-code (@DB-SCH) in a task detail, note, or KB body auto-creates a typed reference.

Hard boundary: these are ADVISORY context links only — no blocking, no gating.

Milestones:
  ○ Add short_code to project_knowledge schema
  ○ Update KB UI to require a 6-character short code
  ○ Update get_task MCP to inject short codes instead of UUIDs in the KB index
  ○ Reference extraction on save (task details, notes, KB bodies)
  ○ Backlink rendering on tasks + KB entries
