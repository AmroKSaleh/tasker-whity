---
id: TDE-360
title: Org layer — admin web UI (org switcher, org environments, member roles, per-env grants)
status: done
priority: medium
section: core-app
group: environments-org
order: 71
updated_at: 2026-07-25T07:41:59.616Z
---

PARKED build seed (Phase-2 B2B). The web surface on top of the security spine seed. Design in KB "Org/RBAC layer — design + cost analysis (TDE-302)".

Scope: extend the TDE-356 environment switcher to also switch ownership context (Personal ↔ KeyLife); an org admin can create org-owned environments, see/manage members and their roles, and grant each member access to SPECIFIC org environments (env-scoped grants). Reuses the TDE-358 Environments management page patterns. Depends on the security-spine seed being built first.

--- SLICE 1 BUILT + DEPLOYED (2026-07-05): orgs + org environments + switching ---
Decisions: dedicated /organizations page for org mgmt; /environments stays personal-only; active context = active environment (carries org_id), no new pointer.
Key move: useEnvironments + lib/environments.refreshEnvironments now query environments WITHOUT a user_id filter → RLS returns personal + accessible org envs automatically. Added org_id to selects.
New: lib/organizations.js (createOrganization/renameOrganization; delete deferred), store organizations[] in useEnvironmentStore, pages/OrganizationsPage.jsx (/organizations route), components/environments/OrgCard.jsx + OrgEnvRow.jsx. createEnvironment(name,color,orgId) + deleteEmptyEnvironment(). reorderEnvironments now persist+refetch (no partial-store clobber).
Switcher: groups envs Personal + per-org, + "Manage organizations…" link. EnvironmentsPage list filtered to personal; reassign dropdown grouped via optgroups (can move projects into org envs).
Build clean; deployed smarttasksxdd.netlify.app. Personal behavior unchanged (no orgs yet → RLS returns same personal envs).
SLICE 2 (next): members + per-env grants + invitations — needs a small backend piece (clients can't look up users by email safely) = ties into TDE-361.
PENDING: user live test of slice 1.

--- REORG BOARD BUILT + DEPLOYED (2026-07-05) ---
User request: move projects between envs + move envs between orgs. Chose a redesigned drag-between-columns view + grants CLEARED on env move.
/environments page REDESIGNED into a kanban: owner lanes (Personal + each org), columns = environments, project cards drag between any columns → moveProjectToEnvironment. Each column header: color dot+palette, inline rename, owner <select> (move env to org/personal via moveEnvironmentToOrg → clears environment_grants + sets org_id), delete-if-empty (deleteEmptyEnvironment). "+ environment" per lane. Unassigned column (draggable) shown only if orphaned projects exist.
New: components/environments/ReorgBoard.jsx + ReorgColumn.jsx (exports ProjectCard). lib/environments.moveEnvironmentToOrg (clears grants). Old EnvRow list + reassign dropdown removed from the page (EnvRow.jsx now unused/dead). Personal env delete now requires empty (was reassign-to-Default).
Build clean; deployed. Uses @dnd-kit useDraggable/useDroppable + pointerWithin.
PENDING: user live test (drag project between envs; move env between Personal/org; grants clear). Then Slice 2 = members + grants + invitations.
