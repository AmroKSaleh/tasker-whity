---
id: TDE-558
title: "[NOT NOW] [STEAL] Study typed link semantics (predecessor/successor vs I/O)"
status: pending
priority: low
section: azure-boards-mined-ideas
order: 2
updated_at: 2026-07-28T19:43:07.739Z
---

Mined from Azure DevOps Boards' typed link vocabulary: parent/child, predecessor/successor, related, duplicate, tested-by.

WHAT: A STUDY task (not a build). We already have I/O edges + duplicate-merge (merge_task_as_duplicate). Azure's distinct predecessor/successor link — pure ORDERING that is separate from data I/O — is worth examining for how our flows express sequencing. Today ordering in Tasker is entangled with I/O edges; Azure separates "B comes after A" from "B consumes A's output."

WHY: could sharpen how flows model ordering-without-data-dependency. Low priority — it's a lens/research item, output is a recommendation on whether to introduce a non-I/O ordering edge, not code.

NEXT STEP: compare our I/O-edge model against Azure's predecessor/successor + related links; write up whether a pure-ordering edge would help flows or just add complexity. Relates to existing flow ordering (get_flow_order, recompute_flow_steps).
