---
id: TDE-356
title: "Environments 3/5 — Web: switcher in NavigationRail + scope projects/board to active Environment"
status: done
priority: high
section: core-app
order: 86
updated_at: 2026-07-14T23:09:51.590Z
---

Third build step of Environments (TDE-308). Requires 1/5 (DB) done; independent of 2/5 (MCP) but shares the schema.

Behavior (decided, do not reopen): EXCLUSIVE switching — one Environment visible at a time; switching to "Work" hides all other Environments' projects from the rail and board. Today/Focus is NOT scoped here (that is 4/5).

Touch points (all in V2/app):
1. State: extend src/store/useProjectStore.js (persisted Zustand, localStorage key 'tasker-projects') or add a small useEnvironmentStore — holds environments list + activeEnvironmentId. On switch, write active_environment_id to user_settings (server-owned pointer so the MCP can report it in init) AND update local state optimistically.
2. src/components/layout/NavigationRail.jsx: Environment switcher at the top of the project list (projects are dnd-kit sortable items — keep that working within the filtered set). Project list renders only active Environment's projects.
3. src/hooks/useProjects.js createProject(): new projects get the active environment_id.
4. src/components/layout/NewProjectModal.jsx: creation lands in the active Environment (an explicit picker is optional, default = active).
5. Check src/lib/prefetch.js (prefetchBoard/prefetchToday) still makes sense when scoped.

Code style per project IS: JSX not TSX, functional components, Tailwind only, split components over ~150 lines. Frontend deploys to Netlify (smarttasksxdd.netlify.app).

--- BUILT + DEPLOYED (2026-07-04), awaiting live visual verification ---
STALE-FILE-MAP CORRECTION: NavigationRail.jsx is DEAD (never imported). Real files: rail = editorial/AppShell.jsx; project list = pages/HomePage.jsx (/projects). Built there instead.
Switcher placement DECIDED with user: on the /projects page header (not the global rail).
New files: store/useEnvironmentStore.js, hooks/useEnvironments.js, components/home/EnvironmentSwitcher.jsx.
Edits: HomePage.jsx (switcher + visibleProjects filter by activeEnvironmentId + env empty state), useProjects.js (fetch select + createProject stamp env), lib/projectBuilder.js (stamp env), NewProjectModal.jsx GitHub path (stamp env).
Exclusive switching implemented; handleDragEnd stays on full projects array so reorder preserves other envs. prefetch.js unchanged (Today cross-env = TDE-357). Vite build clean; deployed to smarttasksxdd.netlify.app.
KB: "Web: Environment switcher + project scoping (TDE-356)".
PENDING: user visual check on live (core-list change). Meaningful switch/filter test needs 2+ envs (only Default exists now).

--- VERIFIED LIVE + DONE (2026-07-04) ---
User confirmed on live: switcher renders on /projects header; picking "Work" filtered the grid to only the Work-env project; switching back to Default restored the real projects. Exclusive switching works end-to-end. Teardown complete — deleted the "Work Env Demo" project and the temporary "Work" environment (Default now the only env again, 0 projects orphaned). TDE-356 DONE.
