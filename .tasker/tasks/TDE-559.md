---
id: TDE-559
title: "[NOT NOW] [CAUTION] Configurable state-transition / contract-gate rules"
status: pending
priority: low
section: azure-boards-mined-ideas
order: 3
updated_at: 2026-07-28T19:43:08.267Z
---

Mined from Azure DevOps Boards' state-transition rules engine (define which states a work item can move between; gate transitions).

WHAT: Azure lets you configure allowed state transitions and gate them. For Tasker this maps onto our CONTRACT GATES — which we already have. The idea worth borrowing is making gates configurable PER PROJECT via a small rules engine.

WHY CAUTION (not steal): this only earns its place if it serves AGENTS/quality-gating, not human process ceremony. Azure's version exists largely for human workflow governance. Adopt ONLY the slice that makes our contract gates more expressive/configurable; do NOT drag in human-approval-chain / workflow-governance baggage. Risk = building a config surface that pulls us toward "customizable Jira workflow," which is the coordination lane we've CONCEDED (see decision: state-layer-not-executor; execution-layer decision — do not benchmark vs Jira/Linear/Asana).

NEXT STEP: decide whether our existing gate machinery (enable_task_review + bar rules) already covers the useful part. If it does, this task closes as "already have it." Only proceed if there's a concrete per-project gate-config gap.
